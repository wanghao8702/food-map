# 美食地图 v1 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用纯前端单文件做一张手绘 SVG 世界地图，把《风味人间 1·第1集 山海之间》的 14 道美食钉在产地上；点击看插画/介绍/纪录片文案/来源，并能溯源到该集全文。

**Architecture:** 无构建、无后端、无地图 key。`index.html`(原生 JS) 渲染内联/外链 SVG 地图与图钉；数据 `fetch()` 自 `dishes.json` 与 `episodes/fw1-01.json`；美食插画为 `assets/svg/*.svg`。双层地图：世界图(海外单钉 + 中国聚合气泡) → 点气泡钻取到中国近景图。详情走右侧滑出面板，「查看本集全文」打开全文覆盖层并高亮当前菜段落。

**Tech Stack:** 原生 HTML/CSS/JS（ES modules），SVG。验证用：Python 3（`validate.py` 校验数据），浏览器测试页（`tests.html` 跑纯函数断言）。运行：`python -m http.server`。

**前置说明：** 本目录当前不是 git 仓库。Task 1 会 `git init` 以支持「频繁提交」。所有路径相对于 `food-map/`。

---

## 文件结构

```
food-map/
├── index.html              # 应用主体：地图、交互、详情面板、全文覆盖层
├── src/
│   ├── projection.js       # 纯函数：经纬度 → SVG 坐标（可单测）
│   ├── data.js             # 加载 dishes.json / episodes，做分组（world / china）
│   ├── map.js              # 渲染两层地图与图钉、聚合气泡、钻取/返回
│   ├── panel.js            # 右侧详情面板
│   └── fulltext.js         # 本集全文覆盖层 + 锚点高亮/跳转
├── styles.css              # 全站样式（暖色手绘调）
├── dishes.json             # 14 道菜数据（一菜一条）
├── episodes/
│   └── fw1-01.json         # 第1集结构化全文（带段落锚点）
├── assets/
│   ├── svg/                # 14 张扁平填色美食插画
│   ├── world-map.svg       # 手绘世界地图底座
│   └── china-map.svg       # 手绘中国近景地图底座
├── tests.html              # 浏览器内纯函数测试页
├── validate.py             # 数据校验脚本（schema/坐标/引用一致性）
├── PRD-美食地图.md
└── 实现计划-v1.md
```

---

## 数据基准（第1集 14 道菜 · 唯一真源）

下表是 `dishes.json` 的内容来源。`scope` 决定钉在哪层。坐标为近似 WGS84（手绘地图用线性映射，精度示意即可）。源行号指《风味人间/《风味人间1》01 山海之间.md》。

| id | name | region | lng | lat | scope | 源行 |
|----|------|--------|-----|-----|-------|------|
| fw1-01-dawei-yang | 清炖阿勒泰大尾羊 | 新疆·阿勒泰 | 88.13 | 47.85 | china | 17–29 |
| fw1-01-naitong-rou | 奶桶肉 | 内蒙古·呼伦贝尔 | 118.60 | 48.20 | china | 45 |
| fw1-01-xun-machang | 熏马肉马肠 | 新疆·乌伦古河 | 87.50 | 46.95 | china | 53–61 |
| fw1-01-mizhi-huofang | 蜜汁火方（皖南火腿） | 安徽·南屏村 | 117.90 | 29.82 | china | 77–133 |
| fw1-01-nianzhuan | 凉拌碾转 | 河南·孟津 | 112.43 | 34.83 | china | 149–157 |
| fw1-01-yangyu-jiaotuan | 洋芋搅团 | 甘肃·临洮 | 103.86 | 35.38 | china | 169–197 |
| fw1-01-longxu-sun | 龙须笋炖鸡 | 四川·瓦屋山 | 103.00 | 29.65 | china | 209–257 |
| fw1-01-bachu-mogu | 巴楚蘑菇炒羊肉 | 新疆·巴楚 | 78.55 | 39.78 | china | 225–241 |
| fw1-01-taihu-dazhaxie | 太湖大闸蟹 | 江苏·太湖 | 120.21 | 31.18 | china | 269–321 |
| fw1-01-tuhuangyou | 秃黄油拌饭 | 江苏·苏州 | 120.62 | 31.30 | china | 305–329 |
| fw1-01-sanbei-qiyu | 三杯旗鱼皮 | 中国台湾·台东成功港 | 121.38 | 23.10 | china | 337–353 |
| fw1-01-iberico | 伊比利亚火腿 | 西班牙·韦尔瓦 | -6.93 | 37.72 | world | 97–117 |
| fw1-01-aligot | 阿力高 Aligot | 法国·南部奥布拉克 | 2.85 | 44.62 | world | 185 |
| fw1-01-holland-crab | 大闸蟹（入侵物种） | 荷兰·艾瑟尔湖 | 5.40 | 52.70 | world | 293–301 |

统计：world = 3，china = 11。世界图上 11 条 china 记录聚合成一个气泡。

---

### Task 1: 项目骨架 + 数据校验脚手架

**Files:**
- Create: `validate.py`
- Create: `dishes.json`（先放空数组占位，Task 2 填充）
- Create: `episodes/fw1-01.json`（先放最小结构）
- Create: `assets/svg/.keep`、`styles.css`（空）、`src/` 占位

- [ ] **Step 1: 初始化 git 以支持频繁提交**

Run:
```bash
cd "D:/My Obsidian/ing/poetry map/food-map" && git init && printf ".superpowers/\n.venv/\n__pycache__/\n" > .gitignore
```
Expected: `Initialized empty Git repository`

- [ ] **Step 2: 写校验脚本 `validate.py`**

```python
#!/usr/bin/env python3
"""校验 dishes.json 与 episodes/*.json 的结构与引用一致性。"""
import json, os, sys, glob

ROOT = os.path.dirname(os.path.abspath(__file__))
REQUIRED = ["id","name","season","episode","episodeTitle","region",
            "lng","lat","scope","intro","quote","episodeAnchor","svg"]

def fail(msg): print("FAIL:", msg); sys.exit(1)

def main():
    with open(os.path.join(ROOT,"dishes.json"),encoding="utf-8") as f:
        dishes = json.load(f)
    if not isinstance(dishes, list) or not dishes:
        fail("dishes.json 应为非空数组")
    ids = set()
    for d in dishes:
        for k in REQUIRED:
            if k not in d: fail(f"{d.get('id','?')} 缺字段 {k}")
        if d["id"] in ids: fail(f"重复 id {d['id']}")
        ids.add(d["id"])
        if d["scope"] not in ("world","china"): fail(f"{d['id']} scope 非法")
        if not (-180 <= d["lng"] <= 180): fail(f"{d['id']} lng 越界")
        if not (-90 <= d["lat"] <= 90): fail(f"{d['id']} lat 越界")
        svg = os.path.join(ROOT,"assets","svg",d["svg"])
        if not os.path.exists(svg): fail(f"{d['id']} 缺 svg 文件 {d['svg']}")
        # 锚点必须存在于对应集 JSON
        ep = os.path.join(ROOT,"episodes",f"fw{d['season']}-{d['episode']:02d}.json")
        if not os.path.exists(ep): fail(f"{d['id']} 缺集文件 {ep}")
        with open(ep,encoding="utf-8") as f:
            anchors = {p["anchor"] for p in json.load(f)["paragraphs"] if "anchor" in p}
        if d["episodeAnchor"] not in anchors:
            fail(f"{d['id']} 的 episodeAnchor {d['episodeAnchor']} 在 {ep} 中不存在")
    print(f"OK: {len(dishes)} dishes valid")

if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 建占位文件**

```bash
mkdir -p assets/svg episodes src && touch assets/svg/.keep styles.css
echo "[]" > dishes.json
echo '{"id":"fw1-01","title":"山海之间","season":1,"episode":1,"paragraphs":[]}' > episodes/fw1-01.json
```

- [ ] **Step 4: 运行校验，确认对空数据报错（验证脚本本身工作）**

Run: `python validate.py`
Expected: `FAIL: dishes.json 应为非空数组`（退出码 1）

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "chore: 项目骨架 + 数据校验脚本"
```

---

### Task 2: 第1集美食数据 `dishes.json`

**Files:**
- Modify: `dishes.json`（填入全部 14 条）

> 说明：`episodeAnchor` 取值与 Task 3 的段落锚点一一对应（命名约定：`p-<id去掉fw1-01前缀>`）。`svg` 文件名在 Task 6 产出；Task 6 完成前 `validate.py` 会因缺文件报错，属预期。

- [ ] **Step 1: 写入 14 条记录**

```json
[
  {"id":"fw1-01-dawei-yang","name":"清炖阿勒泰大尾羊","season":1,"episode":1,"episodeTitle":"山海之间","region":"新疆·阿勒泰","lng":88.13,"lat":47.85,"scope":"china","intro":"山地放养的阿勒泰大尾羊，当年生羔羊肉质幼嫩酥软、油脂丰沛，清水炖煮即是夏季美味。","quote":"当年生的羔羊，肉质幼嫩酥软，油脂丰沛，浓香清甜，只要清水炖煮，就是难得的夏季美味。","episodeAnchor":"p-dawei-yang","svg":"dawei-yang.svg"},
  {"id":"fw1-01-naitong-rou","name":"奶桶肉","season":1,"episode":1,"episodeTitle":"山海之间","region":"内蒙古·呼伦贝尔","lng":118.60,"lat":48.20,"scope":"china","intro":"呼伦贝尔牧民以卵石炙烤、奶桶密闭增压焖烤羯羊，带皮羊肉汁水丰盈，独具炙烤香。","quote":"卵石内部炙烤，奶桶密闭增压，内外同时加热，带皮焖烤的羊肉汁水丰盈，并带有独特的炙烤香。","episodeAnchor":"p-naitong-rou","svg":"naitong-rou.svg"},
  {"id":"fw1-01-xun-machang","name":"熏马肉马肠","season":1,"episode":1,"episodeTitle":"山海之间","region":"新疆·乌伦古河","lng":87.50,"lat":46.95,"scope":"china","intro":"哈萨克牧民把肥瘦相间的马肋排穿入马肠，低温冻结、烟火脱水、晾挂月余，凝成标志性美食。","quote":"在肠衣的密闭保护下，马肉、肋骨、脂肪协同分解和转化，从容积累风味物质，造就哈萨克族的标志性美食——熏马肉马肠。","episodeAnchor":"p-xun-machang","svg":"xun-machang.svg"},
  {"id":"fw1-01-mizhi-huofang","name":"蜜汁火方（皖南火腿）","season":1,"episode":1,"episodeTitle":"山海之间","region":"安徽·南屏村","lng":117.90,"lat":29.82,"scope":"china","intro":"皖南农家火腿历经四季，取肥瘦相间的上方，调和冰糖蜜汁蒸制四小时，咸鲜与甘甜奇迹相逢。","quote":"上方调和冰糖与蜜汁，蒸制4小时，这是火腿以主角身份的惊艳登场。繁复的制作，酥烂剔透的口感，咸鲜与甘甜，在这里奇迹般相逢。","episodeAnchor":"p-mizhi-huofang","svg":"mizhi-huofang.svg"},
  {"id":"fw1-01-nianzhuan","name":"凉拌碾转","season":1,"episode":1,"episodeTitle":"山海之间","region":"河南·孟津","lng":112.43,"lat":34.83,"scope":"china","intro":"用灌浆未熟的青麦汆炒碾压而成，曾是青黄不接的应急口粮，今以新蒜辣椒花生凉拌尝鲜。","quote":"当季新蒜捣碎成泥，伙同辣椒、花生碎、香油、醋汁，共同烘托这青麦的甜香。","episodeAnchor":"p-nianzhuan","svg":"nianzhuan.svg"},
  {"id":"fw1-01-yangyu-jiaotuan","name":"洋芋搅团","season":1,"episode":1,"episodeTitle":"山海之间","region":"甘肃·临洮","lng":103.86,"lat":35.38,"scope":"china","intro":"陇中高原的高糯性土豆反复捶打拉伸，得韧糯口感，配油泼辣子、韭菜与一勺酸楚浆水。","quote":"洋芋搅团配上油泼辣子、韭菜，尤其那一勺酸楚动人的浆水，酸辣辛香，让熟悉它的西北人口舌生津。","episodeAnchor":"p-yangyu-jiaotuan","svg":"yangyu-jiaotuan.svg"},
  {"id":"fw1-01-longxu-sun","name":"龙须笋炖鸡","season":1,"episode":1,"episodeTitle":"山海之间","region":"四川·瓦屋山","lng":103.00,"lat":29.65,"scope":"china","intro":"瓦屋山深山冷笋刻丝、炭火烘干成龙须笋，冷水复活后浸入鸡汤，鲜嫩清爽，是隆重的宴客菜。","quote":"纤细的笋丝在冷水中也能轻松恢复活力，浸入炖好的鸡汤，鲜嫩清爽。","episodeAnchor":"p-longxu-sun","svg":"longxu-sun.svg"},
  {"id":"fw1-01-bachu-mogu","name":"巴楚蘑菇炒羊肉","season":1,"episode":1,"episodeTitle":"山海之间","region":"新疆·巴楚","lng":78.55,"lat":39.78,"scope":"china","intro":"塔克拉玛干边缘胡杨林里顽强生长的巴楚菇，质地疏松多皱，与羊肉同炒最易吸纳肉香。","quote":"巴楚菇与羊肉同炒，疏松多皱的质地，更容易吸纳肉类受热后产生的肉质香，这让它在汤汁中格外出彩。","episodeAnchor":"p-bachu-mogu","svg":"bachu-mogu.svg"},
  {"id":"fw1-01-taihu-dazhaxie","name":"太湖大闸蟹","season":1,"episode":1,"episodeTitle":"山海之间","region":"江苏·太湖","lng":120.21,"lat":31.18,"scope":"china","intro":"长江下游湖泊是顶级大闸蟹主产区，后期以螺蛳蚌肉育肥，膏肥黄满，滋味馥郁。","quote":"鲜活螺蛳、蚌肉，让大闸蟹膏肥黄满，滋味馥郁。","episodeAnchor":"p-taihu-dazhaxie","svg":"taihu-dazhaxie.svg"},
  {"id":"fw1-01-tuhuangyou","name":"秃黄油拌饭","season":1,"episode":1,"episodeTitle":"山海之间","region":"江苏·苏州","lng":120.62,"lat":31.30,"scope":"china","intro":"只取蟹膏蟹黄低温熬制，盐与雨露辅佐，拌入热饭，每粒米都被膏黄包裹，一时异香满口。","quote":"加姜丝、香醋，耐心搅拌，让每一颗米粒都被膏黄包裹。趁热，一时异香满口。唯有一壶浓茶，才能将这满腔的肥甘化解于无形。","episodeAnchor":"p-tuhuangyou","svg":"tuhuangyou.svg"},
  {"id":"fw1-01-sanbei-qiyu","name":"三杯旗鱼皮","season":1,"episode":1,"episodeTitle":"山海之间","region":"中国台湾·台东成功港","lng":121.38,"lat":23.10,"scope":"china","intro":"黑潮带来的立翅旗鱼，鱼皮富含胶原蛋白，加酱油米酒大火旺炒，临锅放九层塔提味。","quote":"旗鱼皮富含胶原蛋白，加酱油、米酒，大火旺炒。临出锅前，放入九层塔提味，带有浓郁家常风味的三杯旗鱼皮就端上了桌。","episodeAnchor":"p-sanbei-qiyu","svg":"sanbei-qiyu.svg"},
  {"id":"fw1-01-iberico","name":"伊比利亚火腿","season":1,"episode":1,"episodeTitle":"山海之间","region":"西班牙·韦尔瓦","lng":-6.93,"lat":37.72,"scope":"world","intro":"西班牙韦尔瓦山中酒窖陈放的伊比利亚火腿，表面白色菌丝是熟成良好的标志，由切片师仪式般享用。","quote":"熟成之后的火腿，滋味上的表现力竟能激增几十倍。","episodeAnchor":"p-iberico","svg":"iberico.svg"},
  {"id":"fw1-01-aligot","name":"阿力高 Aligot","season":1,"episode":1,"episodeTitle":"山海之间","region":"法国·南部奥布拉克","lng":2.85,"lat":44.62,"scope":"world","intro":"法国南部牧区名菜，牛奶融化土豆泥后拌入大量拉吉奥尔奶酪，加热后韧性延展拉丝。","quote":"阿力高（Aligot）来自牧业发达的法国南部，当地特产拉吉奥尔奶酪，在土豆泥的附着下，加热后韧性得到最大延展。","episodeAnchor":"p-aligot","svg":"aligot.svg"},
  {"id":"fw1-01-holland-crab","name":"大闸蟹（入侵物种）","season":1,"episode":1,"episodeTitle":"山海之间","region":"荷兰·艾瑟尔湖","lng":5.40,"lat":52.70,"scope":"world","intro":"百年前野生蟹苗跨越半个地球，在荷兰最大淡水湖艾瑟尔湖壮大，如今身价不菲，成渔民主业。","quote":"大闸蟹作为入侵物种，在这里发展壮大。如今大闸蟹身价不菲，十几年的时间，售价增长近二十倍。","episodeAnchor":"p-holland-crab","svg":"holland-crab.svg"}
]
```

- [ ] **Step 2: JSON 合法性自检**

Run: `python -c "import json; print(len(json.load(open('dishes.json',encoding='utf-8'))))"`
Expected: `14`

- [ ] **Step 3: 提交**

```bash
git add dishes.json && git commit -m "feat: 第1集 14 道美食数据"
```

---

### Task 3: 第1集结构化全文 `episodes/fw1-01.json`

**Files:**
- Modify: `episodes/fw1-01.json`

> 结构：`paragraphs` 为有序数组，逐段保留原文。美食段落带 `anchor`（与 dishes 的 `episodeAnchor` 对应）与 `dishId`，渲染时高亮并支持跳转。非美食段落不带 anchor。

- [ ] **Step 1: 写入结构化全文**

按下面 schema 写入。`text` 从 `风味人间/《风味人间1》01 山海之间.md` 对应行抄录；为每道菜挑选其代表段落打 anchor（与 Task 2 的 14 个 `p-*` 对应）。

```json
{
  "id": "fw1-01",
  "title": "山海之间",
  "season": 1,
  "episode": 1,
  "paragraphs": [
    {"text": "不同的地理条件，造就了人类千姿百态的生活方式……让我们穿越四季，跨越山海，开始一场采集风味的旅行。"},
    {"text": "/新疆-库尔特/", "marker": true},
    {"anchor": "p-dawei-yang", "dishId": "fw1-01-dawei-yang", "text": "当年生的羔羊，肉质幼嫩酥软，油脂丰沛，浓香清甜，只要清水炖煮，就是难得的夏季美味。"},
    {"text": "/内蒙古-新巴尔虎左旗/", "marker": true},
    {"anchor": "p-naitong-rou", "dishId": "fw1-01-naitong-rou", "text": "卵石内部炙烤，奶桶密闭增压，内外同时加热，带皮焖烤的羊肉汁水丰盈，并带有独特的炙烤香。"},
    {"text": "/新疆-乌伦古河南岸/", "marker": true},
    {"anchor": "p-xun-machang", "dishId": "fw1-01-xun-machang", "text": "在肠衣的密闭保护下，马肉、肋骨、脂肪协同分解和转化，从容积累风味物质，造就哈萨克族的标志性美食——熏马肉马肠。"},
    {"text": "/安徽-南屏村/", "marker": true},
    {"anchor": "p-mizhi-huofang", "dishId": "fw1-01-mizhi-huofang", "text": "上方调和冰糖与蜜汁，蒸制4小时……咸鲜与甘甜，在这里奇迹般相逢。"},
    {"text": "/西班牙-韦尔瓦/", "marker": true},
    {"anchor": "p-iberico", "dishId": "fw1-01-iberico", "text": "熟成之后的火腿，滋味上的表现力竟能激增几十倍。"},
    {"text": "/河南-孟津/", "marker": true},
    {"anchor": "p-nianzhuan", "dishId": "fw1-01-nianzhuan", "text": "当季新蒜捣碎成泥，伙同辣椒、花生碎、香油、醋汁，共同烘托这青麦的甜香。"},
    {"text": "/法国/", "marker": true},
    {"anchor": "p-aligot", "dishId": "fw1-01-aligot", "text": "阿力高（Aligot）来自牧业发达的法国南部……加热后韧性得到最大延展。"},
    {"text": "/甘肃/", "marker": true},
    {"anchor": "p-yangyu-jiaotuan", "dishId": "fw1-01-yangyu-jiaotuan", "text": "洋芋搅团配上油泼辣子、韭菜，尤其那一勺酸楚动人的浆水，酸辣辛香，让熟悉它的西北人口舌生津。"},
    {"text": "/四川-瓦屋山/", "marker": true},
    {"anchor": "p-longxu-sun", "dishId": "fw1-01-longxu-sun", "text": "纤细的笋丝在冷水中也能轻松恢复活力，浸入炖好的鸡汤，鲜嫩清爽。"},
    {"text": "/新疆-巴楚/", "marker": true},
    {"anchor": "p-bachu-mogu", "dishId": "fw1-01-bachu-mogu", "text": "巴楚菇与羊肉同炒，疏松多皱的质地，更容易吸纳肉类受热后产生的肉质香。"},
    {"text": "/江苏-太湖/", "marker": true},
    {"anchor": "p-taihu-dazhaxie", "dishId": "fw1-01-taihu-dazhaxie", "text": "鲜活螺蛳、蚌肉，让大闸蟹膏肥黄满，滋味馥郁。"},
    {"text": "/荷兰-艾瑟尔湖/", "marker": true},
    {"anchor": "p-holland-crab", "dishId": "fw1-01-holland-crab", "text": "大闸蟹作为入侵物种，在这里发展壮大……售价增长近二十倍。"},
    {"text": "/江苏-苏州/", "marker": true},
    {"anchor": "p-tuhuangyou", "dishId": "fw1-01-tuhuangyou", "text": "加姜丝、香醋，耐心搅拌，让每一颗米粒都被膏黄包裹。趁热，一时异香满口。"},
    {"text": "/中国台湾-成功港/", "marker": true},
    {"anchor": "p-sanbei-qiyu", "dishId": "fw1-01-sanbei-qiyu", "text": "旗鱼皮富含胶原蛋白，加酱油、米酒，大火旺炒……带有浓郁家常风味的三杯旗鱼皮就端上了桌。"},
    {"text": "环球同此凉热，边界逐渐模糊……山川依旧，风味不改。"}
  ]
}
```

- [ ] **Step 2: 校验锚点齐全（14 个 anchor，与 dishes 一一对应）**

Run: `python -c "import json;a={p['anchor'] for p in json.load(open('episodes/fw1-01.json',encoding='utf-8'))['paragraphs'] if 'anchor' in p};print(len(a))"`
Expected: `14`

- [ ] **Step 3: 提交**

```bash
git add episodes/fw1-01.json && git commit -m "feat: 第1集结构化全文与段落锚点"
```

---

### Task 4: 坐标投影纯函数 + 浏览器测试页

**Files:**
- Create: `src/projection.js`
- Create: `tests.html`

> 手绘地图用「线性映射」：给定地图 SVG 的可视经纬度范围与 viewBox 像素范围，把 (lng,lat) 映射到 (x,y)。世界图与中国图各有一组参数。

- [ ] **Step 1: 写测试页 `tests.html`（先失败）**

```html
<!DOCTYPE html><html><head><meta charset="utf-8"><title>tests</title></head>
<body><pre id="out"></pre>
<script type="module">
import { project } from './src/projection.js';
const out = document.getElementById('out');
let pass = 0, fail = 0;
function eq(name, a, b, tol=0.5){
  if (Math.abs(a-b) <= tol){ pass++; out.textContent += `PASS ${name}\n`; }
  else { fail++; out.textContent += `FAIL ${name}: got ${a}, want ${b}\n`; }
}
// 世界图参数：经度[-180,180]→x[0,1000]，纬度[85,-60]→y[0,500]
const W = { lngMin:-180, lngMax:180, latTop:85, latBottom:-60, width:1000, height:500 };
let p = project(0, 12.5, W);          // 经度0→中点x=500；纬度12.5(范围中点)→y=250
eq('world-center-x', p.x, 500);
eq('world-center-y', p.y, 250);
p = project(180, 85, W);              // 右上角
eq('world-topright-x', p.x, 1000);
eq('world-topright-y', p.y, 0);
out.textContent += (fail===0)?`\nALL PASS (${pass})`:`\n${fail} FAILED`;
</script></body></html>
```

- [ ] **Step 2: 在浏览器打开确认失败**

Run: `python -m http.server 8000`，浏览器打开 `http://localhost:8000/tests.html`
Expected: 报错 `project is not a function` / 模块加载失败

- [ ] **Step 3: 实现 `src/projection.js`**

```js
// 经纬度 → SVG 坐标的线性映射。cfg: {lngMin,lngMax,latTop,latBottom,width,height}
export function project(lng, lat, cfg) {
  const x = (lng - cfg.lngMin) / (cfg.lngMax - cfg.lngMin) * cfg.width;
  const y = (cfg.latTop - lat) / (cfg.latTop - cfg.latBottom) * cfg.height;
  return { x, y };
}
```

- [ ] **Step 4: 刷新 `tests.html` 确认全部通过**

Expected: 页面显示 `ALL PASS (4)`

- [ ] **Step 5: 提交**

```bash
git add src/projection.js tests.html && git commit -m "feat: 坐标投影纯函数 + 浏览器测试页"
```

---

### Task 5: SVG 视觉规范 + 复用元件库 + 1 张参考插画

**Files:**
- Create: `assets/svg/_spec.md`（视觉规范）
- Create: `assets/svg/tuhuangyou.svg`（参考样板：秃黄油拌饭）

> 先把规范钉死，后续 13 张照此产出，保证一致。

- [ ] **Step 1: 写视觉规范 `assets/svg/_spec.md`**

```markdown
# 美食 SVG 视觉规范（扁平填色）
- 画布：viewBox="0 0 120 120"，主体居中，四周留白约 12。
- 描边：统一深棕 #7a2a1c，stroke-width 1.6，stroke-linejoin/linecap=round。
- 调色板（暖色）：主红 #e8623f / 亮橙 #f0a05a / 黄 #f4c95d / 绿 #7fa860 / 米白 #fbf3e0 / 棕 #b9763c。
- 风格：实色填充 + 同色系深色描边；高光用 1–2 个白色小圆/弧。无渐变、无阴影、无外部字体。
- 容器元件：碗(ellipse+口沿弧)、盘(扁椭圆)、砧板、串签；从这些基础形状组合具体菜品。
- 每个文件是自包含 `<svg>`，不引用外部资源。
```

- [ ] **Step 2: 画参考样板 `assets/svg/tuhuangyou.svg`（一碗蟹黄拌饭）**

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120">
  <g stroke="#7a2a1c" stroke-width="1.6" stroke-linejoin="round" stroke-linecap="round">
    <ellipse cx="60" cy="78" rx="34" ry="10" fill="#fbf3e0"/>
    <path d="M26,74 Q60,92 94,74 L92,80 Q60,98 28,80 Z" fill="#e8d6b0"/>
    <path d="M30,70 Q60,52 90,70 Q60,82 30,70 Z" fill="#fbf3e0"/>
    <circle cx="48" cy="66" r="3" fill="#f4c95d"/><circle cx="60" cy="62" r="3.5" fill="#f0a05a"/>
    <circle cx="72" cy="66" r="3" fill="#f4c95d"/><circle cx="56" cy="69" r="2.5" fill="#e8623f"/>
    <circle cx="66" cy="69" r="2.5" fill="#e8623f"/>
    <path d="M40,60 Q44,50 52,54" fill="none"/>
    <circle cx="60" cy="60" r="1.6" fill="#fff" stroke="none"/>
  </g>
</svg>
```

- [ ] **Step 3: 浏览器目检**

Run: 打开 `http://localhost:8000/assets/svg/tuhuangyou.svg`
Expected: 看到一碗暖色蟹黄拌饭，描边统一、无报错。

- [ ] **Step 4: 提交**

```bash
git add assets/svg/_spec.md assets/svg/tuhuangyou.svg && git commit -m "feat: SVG 视觉规范 + 秃黄油参考插画"
```

---

### Task 6: 绘制其余 13 张美食 SVG

**Files:**
- Create: `assets/svg/{dawei-yang,naitong-rou,xun-machang,mizhi-huofang,nianzhuan,yangyu-jiaotuan,longxu-sun,bachu-mogu,taihu-dazhaxie,sanbei-qiyu,iberico,aligot,holland-crab}.svg`

> 全部遵循 `_spec.md`：viewBox 120×120、统一描边 #7a2a1c/1.6、暖色板。形象提示：大尾羊=羊+大尾、奶桶肉=木桶+肉块、熏马肠=串挂香肠、蜜汁火方=方块火腿带蜜光、碾转=青麦碗、洋芋搅团=土豆泥碗+红油、龙须笋炖鸡=砂锅笋丝、巴楚蘑菇=蘑菇+羊肉片、太湖大闸蟹=整蟹、三杯旗鱼皮=盘+鱼皮卷+九层塔、伊比利亚火腿=带蹄火腿腿、阿力高=拉丝土豆泥勺、荷兰大闸蟹=蟹+水波。

- [ ] **Step 1: 逐个绘制 13 张 SVG**

每张为自包含 `<svg viewBox="0 0 120 120">`，结构与 Task 5 样板同构（容器元件 + 主体 + 描边 + 高光）。文件名严格匹配 `dishes.json` 的 `svg` 字段。

- [ ] **Step 2: 目检画廊**

创建临时 `gallery.html`（列出 14 张 svg），打开 `http://localhost:8000/gallery.html`
Expected: 14 张风格一致、可辨识、无渲染错误。检查后删除 `gallery.html`。

- [ ] **Step 3: 跑数据校验（此时 svg 齐全，应通过）**

Run: `python validate.py`
Expected: `OK: 14 dishes valid`

- [ ] **Step 4: 提交**

```bash
git add assets/svg/*.svg && git commit -m "feat: 第1集 13 张美食插画 + 全量数据校验通过"
```

---

### Task 7: 手绘世界地图底座 + 世界视图渲染（含中国聚合气泡）

**Files:**
- Create: `assets/world-map.svg`
- Create: `src/data.js`、`src/map.js`、`styles.css`(填充)、`index.html`

- [ ] **Step 1: 画手绘世界地图 `assets/world-map.svg`**

`viewBox="0 0 1000 500"`，米黄底 `#f4ecd8`、棕色海岸线 `#b9985a`，示意性大陆轮廓（含欧亚、北美、非洲、大洋洲）。是底图，不含图钉。

- [ ] **Step 2: 写 `src/data.js`（加载+分组）**

```js
export async function loadData() {
  const dishes = await (await fetch('./dishes.json')).json();
  return {
    all: dishes,
    world: dishes.filter(d => d.scope === 'world'),
    china: dishes.filter(d => d.scope === 'china'),
  };
}
```

- [ ] **Step 3: 写 `index.html` 骨架 + 世界图容器**

```html
<!DOCTYPE html><html lang="zh"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>风味地图 · 山海之间</title><link rel="stylesheet" href="styles.css"></head>
<body>
<header><h1>风味地图</h1><button id="back" hidden>← 返回世界</button></header>
<main id="stage"><svg id="map" viewBox="0 0 1000 500" preserveAspectRatio="xMidYMid meet"></svg></main>
<script type="module" src="src/map.js"></script>
</body></html>
```

- [ ] **Step 4: 写 `src/map.js`（渲染世界图：海外单钉 + 中国气泡）**

```js
import { loadData } from './data.js';
import { project } from './projection.js';

const WORLD = { lngMin:-180, lngMax:180, latTop:85, latBottom:-60, width:1000, height:500, base:'./assets/world-map.svg' };
const CHINA = { lngMin:73, lngMax:135, latTop:54, latBottom:18, width:1000, height:500, base:'./assets/china-map.svg' };
const svg = document.getElementById('map');
const back = document.getElementById('back');
let store, onPick = ()=>{};

const NS='http://www.w3.org/2000/svg';
function el(t,a){const e=document.createElementNS(NS,t);for(const k in a)e.setAttribute(k,a[k]);return e;}

async function setBase(cfg){
  const txt = await (await fetch(cfg.base)).text();
  svg.innerHTML = '';
  const g = el('g',{}); g.innerHTML = txt.replace(/<\/?svg[^>]*>/g,''); svg.appendChild(g);
}
function pin(d,cfg){
  const {x,y}=project(d.lng,d.lat,cfg);
  const p=el('circle',{cx:x,cy:y,r:7,class:'pin','data-id':d.id});
  p.addEventListener('click',()=>onPick(d));
  const t=el('title',{}); t.textContent=d.name; p.appendChild(t);
  svg.appendChild(p);
}
export async function renderWorld(){
  await setBase(WORLD); back.hidden=true;
  store.world.forEach(d=>pin(d,WORLD));
  // 中国聚合气泡（钉在中国大致中心 105,36）
  const {x,y}=project(105,36,WORLD);
  const g=el('g',{class:'cluster'});
  g.appendChild(el('circle',{cx:x,cy:y,r:20}));
  const label=el('text',{x,y:y-2,'text-anchor':'middle'}); label.textContent='中国';
  const cnt=el('text',{x,y:y+12,'text-anchor':'middle',class:'cnt'}); cnt.textContent=store.china.length+'道';
  g.append(label,cnt); g.addEventListener('click',renderChina); svg.appendChild(g);
}
export async function renderChina(){
  await setBase(CHINA); back.hidden=false;
  store.china.forEach(d=>pin(d,CHINA));
}
export function init(onPickCb){ onPick = onPickCb || onPick; }

back.addEventListener('click',renderWorld);
loadData().then(s=>{ store=s; renderWorld(); });
```

- [ ] **Step 5: 写 `styles.css`（暖色手绘调 + 图钉/气泡样式）**

```css
/* Claude 暖色调：暖米纸底 + 赤陶/珊瑚橙主色 + 暖深棕文字 */
:root{
  --paper:#F0EEE6;     /* Claude 暖米纸背景 */
  --paper-2:#FAF9F5;   /* 更浅卡片底 */
  --band:#E8E2D2;      /* 顶栏/分隔暖色 */
  --line:#D9D2C0;      /* 描边/分割线 */
  --accent:#D97757;    /* Claude orange（珊瑚橙）主色 */
  --accent-2:#CC785C;  /* 赤陶（hover/次级） */
  --ink:#3D3929;       /* 暖深棕主文字 */
  --ink-soft:#6B6453;  /* 次级文字 */
}
*{box-sizing:border-box}
body{margin:0;font-family:"Noto Serif SC",serif;background:var(--paper);color:var(--ink)}
header{display:flex;align-items:center;gap:12px;padding:10px 18px;background:var(--band);border-bottom:2px solid var(--line)}
header h1{font-size:18px;margin:0;color:var(--ink)}
#back{background:var(--accent);color:#fff;border:none;border-radius:14px;padding:4px 12px;cursor:pointer}
#back:hover{background:var(--accent-2)}
#stage{width:100%;height:calc(100vh - 56px)}
#map{width:100%;height:100%;display:block}
.pin{fill:var(--accent);stroke:#fff;stroke-width:1.5;cursor:pointer;transition:r .1s}
.pin:hover{r:10}
.cluster circle{fill:var(--accent);opacity:.92;cursor:pointer}
.cluster text{fill:#fff;font-size:11px;font-weight:700;pointer-events:none}
.cluster .cnt{font-size:9px;font-weight:400}
```

- [ ] **Step 6: 浏览器验证世界视图**

Run: 打开 `http://localhost:8000/`
Expected: 手绘世界图上，西班牙/法国/荷兰各 1 个图钉；中国位置 1 个「中国 / 11道」气泡；hover 图钉显示菜名。

- [ ] **Step 7: 提交**

```bash
git add index.html styles.css src/data.js src/map.js assets/world-map.svg && git commit -m "feat: 手绘世界地图 + 世界视图(海外单钉+中国聚合气泡)"
```

---

### Task 8: 中国近景地图 + 钻取/返回

**Files:**
- Create: `assets/china-map.svg`
- Modify: `src/map.js`（`renderChina` 已在 Task 7 写好，本任务补底图与联调）

- [ ] **Step 1: 画手绘中国近景图 `assets/china-map.svg`**

`viewBox="0 0 1000 500"`，与世界图同风格；示意性中国轮廓（含台湾、海南），覆盖经度约 73–135、纬度约 18–54，与 `src/map.js` 的 `CHINA` 投影参数一致。

- [ ] **Step 2: 浏览器验证钻取**

Run: 打开 `http://localhost:8000/`，点「中国」气泡
Expected: 切换到中国近景图，11 道中国菜按省份位置散开为图钉（新疆偏左、台湾偏右下等）；顶部出现「← 返回世界」；点它回到世界图。

- [ ] **Step 3: 校验无重叠遮挡（目检）**

Expected: 江苏太湖与苏州两钉距离近但都可点（如完全重叠，临时在 `pin()` 对相同坐标做 ±6px 放射偏移）。若需要，补：

```js
// map.js：渲染中国菜前对同坐标做放射偏移
function spread(list){
  const seen={};
  return list.map(d=>{const k=d.lng.toFixed(2)+','+d.lat.toFixed(2);
    const n=(seen[k]=(seen[k]||0)+1)-1; const a=n*2.4;
    return {...d, _dx:Math.cos(a)*7*Math.min(n,3), _dy:Math.sin(a)*7*Math.min(n,3)};});
}
```
并在 `pin()` 用 `x+(d._dx||0)`、`y+(d._dy||0)`。

- [ ] **Step 4: 提交**

```bash
git add assets/china-map.svg src/map.js && git commit -m "feat: 中国近景地图 + 钻取/返回"
```

---

### Task 9: 右侧详情面板

**Files:**
- Create: `src/panel.js`
- Modify: `index.html`（引入 panel）、`styles.css`（面板样式）、`src/map.js`（init 回调接面板）

- [ ] **Step 1: `index.html` 增加面板容器与入口**

在 `</main>` 后加：
```html
<aside id="panel" hidden></aside>
<script type="module">
  import { init } from './src/map.js';
  import { openPanel } from './src/panel.js';
  init(openPanel);
</script>
```
并把原来的 `<script type="module" src="src/map.js">` 删除（改由上面统一初始化，避免双重加载）。

- [ ] **Step 2: 写 `src/panel.js`**

```js
const panel = document.getElementById('panel');
let onFulltext = ()=>{};
export function bindFulltext(cb){ onFulltext = cb; }
export function openPanel(d){
  panel.hidden=false;
  panel.innerHTML = `
    <button class="close" aria-label="关闭">×</button>
    <img class="art" src="./assets/svg/${d.svg}" alt="${d.name}">
    <h2>${d.name}</h2>
    <p class="meta">📍${d.region}　|　🎬 风味人间${d.season} · 第${d.episode}集《${d.episodeTitle}》</p>
    <h3>美食介绍</h3><p>${d.intro}</p>
    <h3>纪录片文案</h3><blockquote>${d.quote}</blockquote>
    <button class="ft">查看本集全文</button>`;
  panel.querySelector('.close').onclick=()=>panel.hidden=true;
  panel.querySelector('.ft').onclick=()=>onFulltext(d);
}
document.addEventListener('keydown',e=>{ if(e.key==='Escape') panel.hidden=true; });
```

- [ ] **Step 3: `styles.css` 加面板样式**

```css
#panel{position:fixed;top:56px;right:0;width:360px;max-width:90vw;height:calc(100vh - 56px);
  background:var(--paper-2);border-left:2px solid var(--line);box-shadow:-6px 0 24px rgba(60,40,10,.18);
  padding:18px 20px;overflow:auto;animation:slide .18s ease}
@keyframes slide{from{transform:translateX(40px);opacity:.3}to{transform:none;opacity:1}}
#panel .close{position:absolute;top:8px;right:12px;border:none;background:none;font-size:22px;cursor:pointer;color:var(--accent-2)}
#panel .art{display:block;width:120px;height:120px;margin:6px auto}
#panel h2{text-align:center;color:var(--ink);margin:6px 0}
#panel .meta{text-align:center;color:var(--ink-soft);font-size:12px;margin:0 0 10px}
#panel h3{font-size:13px;color:var(--accent-2);margin:14px 0 4px}
#panel blockquote{margin:0;padding-left:12px;border-left:3px solid var(--accent);color:var(--ink-soft);font-style:italic}
#panel .ft{margin-top:18px;width:100%;background:var(--accent);color:#fff;border:none;border-radius:8px;padding:8px;cursor:pointer}
#panel .ft:hover{background:var(--accent-2)}
```

- [ ] **Step 4: 浏览器验证**

Run: 打开 `http://localhost:8000/`，点任一图钉
Expected: 右侧滑出面板，显示插画/菜名/地点/季集/介绍/文案；Esc 或 × 关闭；地图仍可见可继续点其它钉。

- [ ] **Step 5: 提交**

```bash
git add index.html styles.css src/panel.js src/map.js && git commit -m "feat: 右侧详情面板"
```

---

### Task 10: 本集全文覆盖层（自动定位高亮 + 集内跳转）

**Files:**
- Create: `src/fulltext.js`
- Modify: `index.html`（绑定 bindFulltext）、`styles.css`（覆盖层与高亮样式）

- [ ] **Step 1: `index.html` 绑定全文入口**

把 Task 9 的初始化脚本改为：
```html
<script type="module">
  import { init } from './src/map.js';
  import { openPanel, bindFulltext } from './src/panel.js';
  import { openFulltext } from './src/fulltext.js';
  init(openPanel);
  bindFulltext(openFulltext);
</script>
```
并在 `</aside>` 后加：`<div id="fulltext" hidden></div>`

- [ ] **Step 2: 写 `src/fulltext.js`**

```js
const layer = document.getElementById('fulltext');
const epCache = {};
async function loadEp(season,ep){
  const key=`fw${season}-${String(ep).padStart(2,'0')}`;
  if(!epCache[key]) epCache[key]=await (await fetch(`./episodes/${key}.json`)).json();
  return epCache[key];
}
export async function openFulltext(dish){
  const data = await loadEp(dish.season, dish.episode);
  const body = data.paragraphs.map(p=>{
    if(p.marker) return `<p class="loc">${p.text}</p>`;
    if(p.anchor) return `<p class="seg" id="${p.anchor}" data-dish="${p.dishId}">`+
      `<span class="tag">${dishName(p.dishId)}</span>${p.text}</p>`;
    return `<p>${p.text}</p>`;
  }).join('');
  const nav = data.paragraphs.filter(p=>p.anchor)
    .map(p=>`<button data-go="${p.anchor}">${dishName(p.dishId)}</button>`).join('');
  layer.innerHTML = `<div class="ft-box">
    <header><h2>风味人间${data.season} · 第${data.episode}集《${data.title}》</h2>
    <button class="ft-close">×</button></header>
    <nav class="ft-nav">${nav}</nav><article>${body}</article></div>`;
  layer.hidden=false;
  layer.querySelector('.ft-close').onclick=()=>layer.hidden=true;
  layer.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>focusSeg(b.dataset.go));
  // 进入即定位并高亮当前菜
  requestAnimationFrame(()=>focusSeg(dish.episodeAnchor));

  function dishName(id){ return (window.__dishNames&&window.__dishNames[id])||''; }
}
function focusSeg(anchor){
  const seg=document.getElementById(anchor); if(!seg) return;
  layer.querySelectorAll('.seg.active').forEach(s=>s.classList.remove('active'));
  seg.classList.add('active'); seg.scrollIntoView({behavior:'smooth',block:'center'});
}
```

- [ ] **Step 3: 在 `data.js` 暴露 id→菜名映射（供全文标签用）**

在 `loadData()` 内 `return` 前加：
```js
window.__dishNames = Object.fromEntries(dishes.map(d=>[d.id,d.name]));
```

- [ ] **Step 4: `styles.css` 加覆盖层与高亮**

```css
#fulltext{position:fixed;inset:0;background:rgba(40,25,10,.5);z-index:10;display:flex;justify-content:center;align-items:flex-start;padding:4vh 0}
.ft-box{background:var(--paper-2);width:760px;max-width:94vw;max-height:92vh;border-radius:10px;overflow:auto}
.ft-box header{position:sticky;top:0;display:flex;justify-content:space-between;align-items:center;padding:12px 20px;background:var(--band);border-bottom:2px solid var(--line)}
.ft-box h2{font-size:16px;margin:0;color:var(--ink)}
.ft-close{border:none;background:none;font-size:22px;cursor:pointer;color:var(--accent-2)}
.ft-nav{display:flex;flex-wrap:wrap;gap:6px;padding:10px 20px;border-bottom:1px solid var(--line)}
.ft-nav button{border:1px solid var(--line);background:var(--paper);border-radius:12px;padding:2px 10px;font-size:12px;cursor:pointer;color:var(--ink)}
.ft-nav button:hover{border-color:var(--accent);color:var(--accent-2)}
.ft-box article{padding:16px 24px;line-height:1.9;color:var(--ink)}
.ft-box .loc{color:var(--ink-soft);font-weight:700}
.ft-box .seg .tag{display:inline-block;background:var(--accent);color:#fff;font-size:11px;border-radius:8px;padding:0 6px;margin-right:6px}
.ft-box .seg.active{background:rgba(217,119,87,.22);border-radius:6px;padding:6px 8px;transition:background .3s}
```

- [ ] **Step 5: 浏览器验证全文链路**

Run: 打开 `http://localhost:8000/`，点「秃黄油拌饭」→ 面板「查看本集全文」
Expected: 弹出第1集全文覆盖层；自动滚动到「秃黄油」段并高亮；顶部导航按钮可在 14 道菜段落间跳转，每段带菜名标签；× 关闭。

- [ ] **Step 6: 提交**

```bash
git add index.html styles.css src/fulltext.js src/data.js && git commit -m "feat: 本集全文覆盖层(自动高亮+集内跳转)"
```

---

### Task 11: 集成验收与收尾

**Files:**
- Modify: 按需微调上述文件

- [ ] **Step 1: 跑数据校验**

Run: `python validate.py`
Expected: `OK: 14 dishes valid`

- [ ] **Step 2: 跑纯函数测试**

Run: 浏览器打开 `http://localhost:8000/tests.html`
Expected: `ALL PASS (4)`

- [ ] **Step 3: 按 PRD §9 验收清单逐条手测**

逐项确认：① 世界图海外单钉+中国气泡；② 钻取中国图与返回；③ 详情面板六要素；④ 全文自动高亮+集内跳转；⑤ 14 张插画风格一致；⑥ 控制台零报错（打开 DevTools Console 核对）。

- [ ] **Step 4: README 与运行说明**

创建 `README.md`：写明 `python -m http.server` 启动、目录结构、如何按集扩展（新增 `episodes/fwX-YY.json` + dishes 记录 + svg）。

- [ ] **Step 5: 最终提交**

```bash
git add -A && git commit -m "chore: v1 集成验收通过 + README"
```

---

## 自检对照（计划 vs PRD）

- PRD §4 数据模型 → Task 2/3（dishes.json 全字段 + 集全文锚点）✓
- PRD §5.1 手绘底座 → Task 7/8（world-map.svg / china-map.svg）✓
- PRD §5.2 双层交互+气泡+钻取 → Task 7/8 ✓
- PRD §5.3 右侧面板六要素 → Task 9 ✓
- PRD §5.4 全文自动高亮+集内区分跳转 → Task 10 ✓
- PRD §5.5 SVG 规范+复用元件+批量 → Task 5/6 ✓
- PRD §6 试点=第1集14道 → Task 2 数据基准表 ✓
- PRD §9 验收清单 → Task 11 ✓
- 防重叠（太湖/苏州近钉） → Task 8 Step 3 ✓
